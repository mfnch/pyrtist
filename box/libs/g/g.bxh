// Header file for the g library

Window = Ptr
([)@Window ? "window_begin"

Window.Save = Void
(])@Window.Save ? "window_save_end"

Line = (Int, Point)

R = (Char -> Int -> Real)
P = ((R, R) -> Point)

([)@Line ? "line_begin"
P@Line ? "line_point"

