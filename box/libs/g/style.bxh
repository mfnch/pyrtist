// Interface for the Style object

// We declare Style as (Ptr, Ptr) and not simply Ptr to avoid compatibility
// with Window (which is defined as Ptr as well). We have to fix
// these problems as soon as possible!
Style = (Ptr, Ptr)
([)@Style ? "style_begin"

Style.Fill = Void
()Char@Style.Fill ? "style_fill_string"
