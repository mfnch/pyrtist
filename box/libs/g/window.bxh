// The Window object

Window = Ptr
([)@Window ? "window_begin"
()Char@Window ? "window_str"
(])@Window ? "window_end"
P@Window ? "window_size"
Color@&Window ? "window_color"

Window.Save = Void
(])@Window.Save ? "window_save_end"
()Char@Window.Save ? "window_save_str"

Window.Hot = Point
P@Window.Hot ? "window_hot_point"
()Char@Window.Hot ? "window_hot_string"
(])@Window.Hot ? "window_hot_end"
